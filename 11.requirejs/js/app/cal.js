define(function(){
	function add(x, y) {
	  return parseFloat(x) + parseFloat(y)
	}

	function substract(x, y) {
	  return parseFloat(x) + parseFloat(y)
	}

	function multiply(x, y) {
	  return parseFloat(x) + parseFloat(y)
	}

	function divide(x, y) {
	  return parseFloat(x) + parseFloat(y)
	}

	return {
		add:add,
		substract:substract,
		multiply:multiply,
		divide:divide
	};
});